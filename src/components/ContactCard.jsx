import React from "react";

function ContactCard({ name, number }) {
  return (
    <div>
      <img src="" />
      <p>{name}</p>
      <small>{number}</small>
      <button>X</button>
    </div>
  );
}

export default ContactCard;
